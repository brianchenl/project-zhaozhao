{
  "name": "project-zhaozhao",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "昭昭计划 Project Zhaozhao — 四语言历史资料档案",
  "license": "MIT",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "sync": "astro sync",
    "check:content": "node scripts/check-content.mjs",
    "check:dist": "node scripts/check-dist.mjs",
    "check:sources": "node scripts/check-sources.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check:content && npm test && npm run check && npm run build && npm run check:dist"
  },
  "dependencies": {
    "astro": "7.3.1",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "cheerio": "1.2.0",
    "typescript": "6.0.3",
    "yaml": "2.9.0"
  }
}
